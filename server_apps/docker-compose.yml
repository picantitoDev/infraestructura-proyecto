version: '3.8'

services:
  jenkins:
    build: .
    container_name: jenkins
    ports:
      - 3000:8080
      - 50000:50000
    volumes:
      - jenkins_home:/var/jenkins_home
    environment:
      - JENKINS_URL=http://localhost:8080/

volumes:
  jenkins_home:
