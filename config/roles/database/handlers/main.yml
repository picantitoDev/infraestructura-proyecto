---
# ==============================================================
# Handlers: acciones reactivas para la base de datos
# ==============================================================

- name: restart postgres container
  ansible.builtin.shell: |
    docker-compose restart postgres
  args:
    chdir: /opt/docker/postgres
  changed_when: false
